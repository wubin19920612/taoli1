# 项目协作规则

## 按功能模块使用任务

- 一个 Codex 任务只处理一个主要功能模块，例如 Astro/浮窗、价差与资金费率告警、新币公告、指数成分监控或服务器运维。
- 开始实现前，先明确本任务所属模块和验收目标。相邻的小修复可以一并完成，但不要让任务无限扩展。
- 如果用户在当前模块尚未交付时提出无直接依赖的新模块需求，应简短提醒：先完成当前模块的验证与交付，再为新模块新建任务。记录新需求即可，不要因此丢失当前工作。
- 模块完成后，应提醒用户在新任务中继续下一个模块，并提供或更新可直接用于新任务的交接文档。

## 模块完成条件

除非用户明确要求只在本地修改，否则“完成”同时包括：

1. 实现功能，并运行与风险范围相称的后端测试、前端测试、类型检查或生产构建。
2. 检查工作区，只暂存本任务文件；保留其他任务和用户的改动及未跟踪产物。
3. 创建 Git 提交并推送当前 GitHub 分支。禁止 force push；Git 命令不使用 `sudo`。
4. 部署前备份线上 `/data/radar.db`，记录备份文件和校验结果。
5. 服务器使用 `git pull --ff-only` 更新，并重新构建、启动 Docker Compose 服务。不得使用 `docker compose down -v`。
6. 检查前后端容器、`/api/health` 以及本次功能的实际接口或页面行为。
7. 最终报告提交号、推送状态、备份、部署版本、线上验证结果和任何残余风险。

详细 Git 与部署步骤分别见 `docs/git-delivery-checklist.md` 和 `docs/linux-deployment.md`。

## 交接要求

- 交接文档应写入 `docs/task-handoff-YYYY-MM-DD-<module>.md`，确保不依赖旧任务上下文也能继续工作。
- 至少记录：目标与范围、分支和基线、已完成功能、关键代码入口、重要业务规则、测试结果、线上状态、已知问题、未跟踪文件和下一步建议。
- 不把 `.env`、密码、API Key、Webhook、SSH 私钥或其他秘密写入仓库。服务器主机与密钥位置只在受控运行环境中使用。

## 项目安全边界

- 不回滚、不删除、不覆盖无法确认归属的现有修改或未跟踪文件。
- 不使用 `git reset --hard`、`git checkout -- <file>` 等破坏性回退命令。
- 涉及 Hyperliquid 时必须保留具体 DEX 和原始市场信息，不能只按规范化 ticker 匹配。
- 涉及交易判断时必须明确资金费率周期、双方成交额、实际可成交价格、手续费、市场倍率和数据是否为预估。
